# 系统设计

## 顶层设计

目前系统架构为三层
    1. 上层交互层 (网页、插件等)
    2. 中层服务层 (处理业务逻辑、调用本地服务)
    3. 底层文件层 (存储提示词正文、配置文件、sqlite存元信息 等)

约束如下：
    - 只能逐层调用
    - 不能跨层直写

### 核心模块

1. 提示词管理

## 模块具体设计

#### 目标

只解决提示词的 **增删改查、检索、复制、调用vscode打开对应文件** 。
